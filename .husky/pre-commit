#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if [ "$HUSKY_GIT_PARAMS" = "--no-verify" ]; then
  echo "✅ Хук пропущен (использован --no-verify)"
  exit 0
fi

# Получаем текущую ветку
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)

# Проверяем, начинается ли ветка с "feature/"
if echo "$CURRENT_BRANCH" | grep -q '^feature/'; then
  echo "🔄 Запускаем npx changeset для ветки $CURRENT_BRANCH"
  npx changeset --empty

  # Ждём появления файла
  sleep 1

  # Получаем путь к последнему файлу
  CHANGESET_FILE=$(find .changeset -type f -name "*.md" | sort -r | head -n 1)

  echo "📄 Найден файл: $CHANGESET_FILE"

  # Получаем сообщение коммита
  COMMIT_MSG=$(git log -1 --pretty=%B)

  # Добавляем минимум 5 строк, если их нет
  FILE_LINES=$(wc -l < "$CHANGESET_FILE")
  while [ $FILE_LINES -lt 5 ]; do
    echo "" >> "$CHANGESET_FILE"
    FILE_LINES=$((FILE_LINES + 1))
  done

  echo "📄 Файл дополнен до 5 строк"

  # Добавляем строку на 2-ю позицию через awk
  awk 'NR == 2 { print "\"test-changelog-vite\": patch" } { print }' "$CHANGESET_FILE" > "$CHANGESET_FILE.tmp" && mv "$CHANGESET_FILE.tmp" "$CHANGESET_FILE"

  # Добавляем сообщение на 5-ю позицию через awk
  awk -v msg="$COMMIT_MSG" 'NR == 5 { print msg } { print }' "$CHANGESET_FILE" > "$CHANGESET_FILE.tmp" && mv "$CHANGESET_FILE.tmp" "$CHANGESET_FILE"

  git add "$CHANGESET_FILE"
  echo "✅ changeset создан и изменён"
else
  echo "ℹ️ Ветка $CURRENT_BRANCH не соответствует pattern"
fi