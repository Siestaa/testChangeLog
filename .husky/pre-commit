#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if [ "$HUSKY_GIT_PARAMS" = "--no-verify" ]; then
  echo "✅ Хук пропущен (использован --no-verify)"
  exit 0
fi

# Получаем текущую ветку
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)

# Проверяем, начинается ли ветка с "feature/"
if echo "$CURRENT_BRANCH" | grep -q '^feature/'; then
  echo "🔄 Запускаем npx changeset для ветки $CURRENT_BRANCH"
  npx changeset --empty

  # Ждём появления файла
  echo "⏳ Ожидаем появления changeset-файла..."
  ATTEMPTS=0
  MAX_ATTEMPTS=10
  while [ -z "$(find .changeset -type f -name \"*.md\")" ] && [ $ATTEMPTS -lt $MAX_ATTEMPTS ]; do
    sleep 1
    ATTEMPTS=$((ATTEMPTS + 1))
  done

  if [ $ATTEMPTS -ge $MAX_ATTEMPTS ]; then
    echo "❌ changeset-файл не создан за $MAX_ATTEMPTS секунд"
    exit 1
  fi

  # Получаем путь к последнему созданному файлу
  CHANGESET_FILE=$(find .changeset -type f -name "*.md" | sort -r | head -n 1)

  # Получаем сообщение коммита
  COMMIT_MSG=$(git log -1 --pretty=%B)

  echo "📄 Изменяем файл: $CHANGESET_FILE"

  # Добавляем строку на 2-ю позицию
  sed -i '' '2i\
"test-changelog-vite": patch
' "$CHANGESET_FILE"

  # Добавляем сообщение на 5-ю позицию
  sed -i '' "5i\\
$COMMIT_MSG
" "$CHANGESET_FILE"

  git add "$CHANGESET_FILE"
  echo "✅ changeset создан и изменён"
else
  echo "ℹ️ Ветка $CURRENT_BRANCH не соответствует pattern 'feature/*' — changeset не создается"
fi