#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Получаем текущую ветку
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)

# Проверяем, определена ли ветка
if [ -z "$CURRENT_BRANCH" ]; then
  echo "❌ Текущая ветка не определена (detached HEAD)"
  exit 0
fi

# Проверяем, начинается ли ветка с "feature/"
if ! echo "$CURRENT_BRANCH" | grep -q '^feature/'; then
  echo "ℹ️ Ветка $CURRENT_BRANCH не соответствует pattern 'feature/*' — изменения не вносятся"
  exit 0
fi

# Проверяем, есть ли флаг --no-verify
if [ "$HUSKY_GIT_PARAMS" = "--no-verify" ]; then
  echo "✅ Хук пропущен (использован --no-verify)"
  exit 0
fi

# Проверяем, является ли коммит merge-коммитом
COMMIT_MSG=$(git log -1 --pretty=%B)
if echo "$COMMIT_MSG" | grep -q '^Merge'; then
  echo "🗑️ Обнаружен merge-коммит — changeset не обрабатывается"
  exit 0
fi

CHANGESET_DIR=$(pwd)/.changeset
CHANGESET_FILE=$(find "$CHANGESET_DIR" -type f -name "*.md" -printf "%T@ %p\n" | sort -n | tail -1 | cut -f2- -d' ')

# Проверяем, существует ли файл
if [ -f "$CHANGESET_FILE" ]; then
  # Проверяем, не пустой ли файл и не состоит ли только из дефисов и пробелов
  FILE_CONTENT=$(cat "$CHANGESET_FILE" | tr -d '[:space:]')

  if [ -z "$FILE_CONTENT" ] || [ "$FILE_CONTENT" = "-----" ]; then
    echo "📄 changeset-файл найден, но он пустой или содержит только '---' — изменения вносятся"
  else
    echo "📄 changeset-файл найден и не пустой — пуш разрешён"
    exit 0
  fi
else
  echo "📄 changeset-файл не найден — можно продолжить"
fi

# Парсим тип коммита
commit_type="${COMMIT_MSG%%:*}"
commit_type=$(echo "$commit_type" | tr '[:upper:]' '[:lower:]')
commit_description=$(echo "$COMMIT_MSG" | sed -E 's/^[^:]+: ?//; s/[[:space:]]+$//')

# Проверяем, является ли тип допустимым
case "$commit_type" in
  patch|minor|major) ;;
  *) commit_type="patch"; echo "📎 Тип коммита не указан или некорректен — установлен тип по умолчанию: $commit_type" ;;
esac

# Проверяем, начинается ли ветка с "feature/"
if echo "$CURRENT_BRANCH" | grep -q '^feature/'; then
  echo "🔄 Выполняем логику changeset для ветки $CURRENT_BRANCH"

  # Проверяем, существует ли файл
  if [ ! -f "$CHANGESET_FILE" ]; then
    echo "❌ changeset-файл не найден — ошибка"
    exit 1
  fi

  # Очищаем файл и создаём 5 строк
  > "$CHANGESET_FILE"
  for i in $(seq 1 5); do
    echo "" >> "$CHANGESET_FILE"
  done

  # Добавляем содержимое через один вызов awk
  awk -v type="$commit_type" -v msg="$commit_description" '
  NR == 1 { print "---\n" }
  NR == 2 { print "\"test-changelog-vite\": " type }
  NR == 3 { print "---" }
  NR == 5 { print msg }
  { print }
  ' "$CHANGESET_FILE" > "$CHANGESET_FILE.tmp" && mv "$CHANGESET_FILE.tmp" "$CHANGESET_FILE"
  awk 'NF' "$CHANGESET_FILE" > "$CHANGESET_FILE.tmp" && mv "$CHANGESET_FILE.tmp" "$CHANGESET_FILE"

  # Добавляем файл и делаем коммит
  git add "$CHANGESET_FILE"
  git commit -m "changelog update" --no-verify
      echo "📤 Выполняем git commit 'changelog update'"
else
  echo "ℹ️ Ветка $CURRENT_BRANCH не соответствует pattern 'feature/*' — changeset не обрабатывается"
fi